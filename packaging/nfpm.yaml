name: cloudagent
arch: ${NFPM_ARCH}
platform: linux
version: ${NFPM_VERSION}
version_schema: semver
release: 1
section: utils
priority: optional
maintainer: Liu Felix
description: CloudAgent terminal CLI
vendor: Liu Felix
homepage: ${NFPM_HOMEPAGE}
license: MIT
contents:
  - src: dist/linux-root/cloudagent
    dst: /usr/local/bin/cloudagent
    file_info:
      mode: 0755
  - src: dist/linux-root/agentd
    dst: /usr/local/bin/agentd
    file_info:
      mode: 0755
  - src: dist/linux-root/README.md
    dst: /usr/share/doc/cloudagent/README.md
    file_info:
      mode: 0644
